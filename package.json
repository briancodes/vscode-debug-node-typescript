{
  "name": "vscode-node-typescript",
  "version": "1.0.0",
  "description": "Node typescript clean build and debug",
  "main": "index.js",
  "dependencies": {},
  "devDependencies": {
    "rimraf": "^2.6.2",
    "typescript": "^2.6.1"
  },
  "scripts": {
    "clean": "rimraf dist/*",
    "tsc": "tsc",
    "cleanbuild": "npm run clean && npm run tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brian",
  "license": "none"
}
